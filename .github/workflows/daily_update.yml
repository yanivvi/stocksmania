name: Daily Stock Update

on:
  schedule:
    # Runs at 6pm Israel time (4pm UTC) on weekdays
    - cron: '0 16 * * 1-5'
  
  # Allow manual trigger
  workflow_dispatch:

jobs:
  update-stocks:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Fetch latest stock data
        run: |
          python main.py daily -s NVDA AAPL MSFT GOOGL AMZN META TSLA AMD INTC BA KO PLTR CRM CRWD PANW LLY NVO JPM V NFLX COST COIN SPOT
      
      - name: Send Telegram notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          MY_HOLDINGS: ${{ secrets.MY_HOLDINGS }}
        run: |
          python telegram_notify.py
      
      - name: Commit updated data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --quiet && git diff --staged --quiet || git commit -m "📈 Daily stock update - $(date +'%Y-%m-%d')"
          git push
